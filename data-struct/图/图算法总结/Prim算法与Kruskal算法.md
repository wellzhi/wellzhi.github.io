# 一. 最小生成树（MST）算法
- 适用：带权无向连通图
- 性质：权值之和最小的生成树
# 1. 普里姆算法（Prim算法）
从某个顶点开始构建生成树；每次`将代价最小的新顶点`纳入生成树，直到所有顶点都纳入为主

- 时间复杂度：O(|V|^2^)
- 适用于边稠密图
  
# 2. 克鲁斯卡尔算法（Kruskal算法）
每次选择一条`权值最小的边`，使这条边的两头连通（原本已经连通的就不选），直到所有结点都连通
- 时间复杂度：O(|E|log~2~|E|)
- 适用于边稀疏图
